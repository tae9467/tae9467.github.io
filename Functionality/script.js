/**
    * Opens the navigation menu
    * @function toggleMobileMenu
    * @param {string} menu - a class that gets put onto the mobile menu
    * @returns {void}
*/
function toggleMobileMenu(menu) {
    menu.classList.toggle('open');
}